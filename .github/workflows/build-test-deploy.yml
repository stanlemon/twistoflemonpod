name: Build, Test, and Deploy

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js environment
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build

      - name: Run tests
        run: npm test

      - name: Run Prettier check
        run: npm run format -- --check

      - name: Check build artifacts
        run: |
          if [ ! -d "_site" ]; then
            echo "Build failed - _site directory not found"
            exit 1
          fi
          echo "Build successful - _site directory created"

      # NOTE: This deploy step is likely unnecessary since Cloudflare Pages
      # automatically deploys from the repository. This can be removed once
      # we're confident Cloudflare Pages is handling all deployments.
      - name: Deploy 🚀
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: JamesIves/github-pages-deploy-action@4a3abc783e1a24aeb44c16e869ad83caf6b4cc23 # v4
        with:
          CLEAN: true
          BRANCH: gh-pages
          FOLDER: _site
